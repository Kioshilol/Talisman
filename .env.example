# Sanity (project TALISMAN, id 786bstzj, https://www.sanity.io/manage)
# When unset, the site builds from mock data in src/data/*.json
PUBLIC_SANITY_PROJECT_ID=786bstzj
PUBLIC_SANITY_DATASET=production

# Cart order intake endpoint (Formspree, Basin, own webhook).
# Empty means the form opens an email draft to info@talisman.by
PUBLIC_ORDER_ENDPOINT=
